# Reference template ONLY; no loader reads this file automatically.
# Keep values empty in Git. Provision the service's restricted SecretProvider later.
FX_LONDON_01_HMAC_V1=
FX_MONITOR_ENDPOINT=
# London-02 uses its own FX_LONDON_02_HMAC_V1 on a separate collector host.
# The sender endpoint reference must resolve to HTTPS /v1/heartbeat.
# No LINE/Email/Push credentials are required on a collector host.
