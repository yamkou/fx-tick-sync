# Reference template ONLY; not loaded automatically and not a credential file.
# Approved provisioning must fill the process/service SecretProvider, never Git.
FX_LONDON_01_HMAC_V1=
FX_LONDON_02_HMAC_V1=
FX_FRANKFURT_01_HMAC_V1=
# Future separately approved webhook/vendor references (not enabled by this template):
FX_PUSH_ENDPOINT=
FX_PUSH_TOKEN=
# LINE Messaging API / Email transport credentials remain unprovisioned.
